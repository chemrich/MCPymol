[project]
name = "mcpymol"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=0.4.1",
    "mcp>=1.26.0",
]

[project.scripts]
mcpymol = "mcpymol.server:main"
